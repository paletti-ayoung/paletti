// const Koa = require('koa');
// const serve = require('koa-static');
// const send = require('koa-send')

// const app = new Koa();

// app.use(serve(__dirname + "./"));

// app.use(async (ctx) => {
//     if (ctx.path == "/") await send(ctx, "/views/home.html", { root: __dirname + "/" })
//     else await send(ctx, "/error.html", { root: __dirname + "/" })
// })

// const port = 5500;
// app.listen(port)